# test1

Proyecto base en Spring Boot 3.4.0 con estructura de paquetes siguiendo la arquitectura hexagonal (application, domain, infrastructure).

## Ejecutar tests

```bash
./mvnw test
```

> **Nota:** El wrapper incluye `.mvn/settings.xml` con la configuración de proxy necesaria para descargar dependencias desde Maven Central.
